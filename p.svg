<svg height="200" width="200" xmlns="http://www.w3.org/2000/svg">
    <rect x="50" y="50" width="100" height="50" fill="lightgray"></rect>
    <text x="85" y="80" fill="black">Start</text>
    <circle cx="100" cy="125" r="25" fill="red"></circle>
	<script>
    const circle = d3.select('circle');
    const text = d3.select('text');
    let interval;

    function changeColor() {
      const color = circle.attr('fill') === 'red' ? 'green' : 'red';
      circle.attr('fill', color);
    }

    function toggleColor() {
      if (interval) {
        clearInterval(interval);
        interval = null;
        text.text('Start');
      } else {
        interval = setInterval(changeColor, 1000);
        text.text('Stop');
      }
    }

    d3.select('rect').on('click', toggleColor);
  </script>
  </svg>
  